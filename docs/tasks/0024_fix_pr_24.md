# Fix PR #24

## Goal
Resolve and fix the issues related to PR #24 ("deps(npm): bump @vitejs/plugin-react from 5.2.0 to 6.0.1 in /frontend").

## Scope
- **Directory**: `docs/tasks/`
- **Files**: `docs/tasks/0024_fix_pr_24.md`

## Context
PR #24 was open, dirty, and stuck because:
1. The codebase had missing repository labels (`dependencies`, `frontend`) that were configured in `.github/dependabot.yml` but did not exist in the GitHub repository.
2. The PR became obsolete since `main` had already been updated to version `6.0.2` of `@vitejs/plugin-react` via a newer pull request (PR #56).

## Granular Instructions
- [x] Research PR #24 details, changed files, and why it is dirty.
- [x] Check the current version of `@vitejs/plugin-react` on `main` branch. (Verified at `6.0.2`).
- [x] Run frontend tests using `bun run test:run` to ensure the version on `main` compiles and works correctly. (All 57 tests passed successfully).
- [x] Identify label missing issues from Dependabot's PR comments.
- [x] Create the missing labels in the repository (`dependencies`, `frontend`, `go`, `ci`, `docker`) using the GitHub CLI.
- [x] Close PR #24 as superseded and document the closure.
- [x] Clean up the obsolete PR remote branch.

## Definition of Done
- [x] Missing repository labels created.
- [x] PR #24 closed.
- [x] `status.md` and docs updated.
